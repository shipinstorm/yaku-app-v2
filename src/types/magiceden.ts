export type ListingInfo = {
    price: number;
};

export type METoken = {
    mintAddress: string;
    supply: number;
    name: string;
    image: string;
    price: number;
    listStatus: string;
};
