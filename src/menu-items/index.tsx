import explore from "./explore";
import applications from "./applications";
import { NavItemType } from "@/types";

const menuItems: { items: NavItemType[] } = {
  items: [explore, applications],
};

export default menuItems;
